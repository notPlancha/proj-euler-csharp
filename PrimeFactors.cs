namespace euler; 

public class PrimeFactors {
    public long Value { get; }
    public PrimeFactors(long value) {
        Value = value;
    }
    // private List<long> BuildFactors() {
    //     
    // }

    private void IsPrime(long value) {
        //checks if number is Prime based on previous primes
        // foreach (long i in primes) {
        //     if (value % i == 0) return false;
        //     if (i >= value) return true;
        // }
        // return true;
    }
}